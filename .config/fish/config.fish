if status is-interactive
    # Commands to run in interactive sessions can go here
end
# nmap colorization handled by ~/.config/fish/functions/nmap.fish (Dracula true-color)
#aliasis
alias smuggler='source ~/smuggler/venv/bin/activate.fish && python ~/smuggler/smuggler.py'
